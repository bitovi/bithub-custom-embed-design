<select class="form-control" can-value="optionValue">
  <option value="">Select {{ option.name }}</option>
  {{#each option.options}}
    <option value="{{id}}">{{name}}</option>
  {{/each}}
</select>
